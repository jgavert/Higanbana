#version 140

uniform vec4 info; // (target.x, target.y, 0.0, 0.0)
uniform sampler2D tex;

out vec4 out_Color;

void main( void ) {
	vec2 po = ( gl_FragCoord.xy / info.xy );
	out_Color = texture(tex, po);
}
