#version 140

uniform vec4 info; // (target.x, target.y, time, 0.0)

out vec4 out_Color;

void main( void ) {
	vec2 po = ( gl_FragCoord.xy / info.xy );
	out_Color = vec4(sin(po.x*po.y*20.0+info.z*2.5)*0.5+0.5, 0.0, 0.0, 1.0);
}
