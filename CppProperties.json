{
  "configurations": [
    {
      "inheritEnvironments": [
        "msvc_x64"
      ],
      "name": "x64-Release",
      "includePath": [
        "${env.INCLUDE}",
        "C:/VulkanSDK/1.1.121.2/Include",
        "${workspaceRoot}/ext/dxc-bin/include",
        "${workspaceRoot}/ext/stb",
        "${workspaceRoot}/ext/imgui",
        "${workspaceRoot}/ext/rapidjson/include",
        "${workspaceRoot}/ext/GSL/include",
        "${workspaceRoot}/ext/cxxopts/include",
        "${workspaceRoot}/ext/winpixeventruntime/Include",
        "${workspaceRoot}/ext/Catch2/single_include",
        "${workspaceRoot}/benchmarks",
        "${workspaceRoot}/core/src",
        "${workspaceRoot}/graphics/src",
        "${workspaceRoot}/test_main/src"
      ],
      "defines": [
        "WIN32",
        "WIN64",
        "UNICODE",
        "_UNICODE",
        "HIGANBANA_PLATFORM_WINDOWS",
        "VK_USE_PLATFORM_WIN32_KHR",
        "CATCH_CONFIG_ENABLE_BENCHMARKING"
      ]
    }
  ]
}