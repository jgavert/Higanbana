#pragma once
/*
#include "core/src/filesystem/filesystem.hpp"
#include "core/src/graphics/desc/formats.hpp"

#if defined(GFX_VULKAN)
// todo
#elif defined(GFX_D3D12)
// todo
#elif defined(GFX_NULL)
// todo
#endif

namespace faze
{
  namespace graphics
  {


    class Instance
    {
    private:
      //backend::InstanceImpl m_instance;

    public:
      Instance();
      bool createInstance(const char* appName, unsigned appVersion = 1, const char* engineName = "faze", unsigned engineVersion = 1);
      GpuDevice createGpuDevice(FileSystem& fs);
      //WindowSurface createSurface(Window& window);
    };
  }
}
*/